/**
 * CareerOS engine registry — what actually powers the product.
 * Luma Agentic Orchestration Hack stack reference: TinyFish (sponsor) for live web Fetch/Search.
 * We adopt Fetch+Search only — not Agent browser bots for LinkedIn Easy Apply.
 */

export { isTinyFishConfigured, tinyfishEngineStatus, tinyfishFetchOne, tinyfishSearch } from "./tinyfish";
